{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Gatsby Preview",
      "url": "http://localhost:{config:leadCMS.gatsbyPort}",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "start-gatsby-server"
    }
  ]
}